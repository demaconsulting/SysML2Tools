---
# Query Subsystem Requirements
#
# PURPOSE:
# - Define requirements for the SysML2Tools Core Query subsystem
# - The Query subsystem exposes a public, reusable model-analysis API over an already-loaded
#   SysML semantic workspace
# - QueryEngine implements the 12 analysis verbs; QueryResult/QueryResultRenderer/
#   QueryResultSerializerContext implement the shared output model; QueryResultExporter writes
#   rendered output to files
# - Requirements describe observable library behavior, not CLI parsing or console reporting

sections:
  - title: Query Subsystem Requirements
    requirements:
      - id: SysML2Tools-Core-Query-Uses
        title: >-
          The Query subsystem shall report a target element's outgoing semantic relationships
          through the `uses` verb, including supertypes, typing, imports, and other resolved
          outward references, excluding stdlib targets unless `IncludeStdlib` is supplied.
        justification: |
          "What does this element depend on?" is a foundational model-comprehension question.
          Exposing the semantic index's outgoing-edge view directly answers it without forcing
          library callers to traverse the semantic model manually.
        tests:
          - Uses_ReportsOutgoingSupertypeTypingImportEdges

      - id: SysML2Tools-Core-Query-UsedBy
        title: >-
          The Query subsystem shall report a target element's incoming semantic relationships
          through the `used-by` verb by using the semantic index's reverse lookup.
        justification: |
          The reverse of `uses` is required for change-impact assessment and traceability review;
          callers need a first-class answer to "what depends on this element?".
        tests:
          - UsedBy_ReportsIncomingReferences

      - id: SysML2Tools-Core-Query-Dependencies
        title: >-
          The Query subsystem shall combine the target element's outgoing and incoming
          relationships into one `dependencies` result, tagging each entry with
          `Outgoing`/`Incoming` direction and rendering Markdown as direction-grouped prose
          bullets rather than the shared table used by other verbs.
        justification: |
          A combined dependency view is a distinct output need from the separate `uses` and
          `used-by` queries. Reusing both traversals while tagging entry direction avoids
          duplicate analysis logic and keeps JSON and Markdown representations aligned.
        tests:
          - Dependencies_CombinesOutgoingAndIncoming_ReportsBothDirections
          - Dependencies_NoOutgoingReferences_ReportsProseLineInsteadOfBulletList
          - Dependencies_NoIncomingReferences_ReportsProseLineInsteadOfBulletList
          - Dependencies_MarkdownOutput_ContainsNoTable
          - RenderMarkdown_DependenciesVerb_RendersBulletProseNotTable
          - RenderMarkdown_DependenciesVerb_EmptyOutgoingAndIncoming_ReportsBothProseLines
          - RenderJson_DependenciesVerb_IncludesDirectionField
          - RenderJson_NonDependenciesVerb_DirectionFieldOmittedFromOutput

      - id: SysML2Tools-Core-Query-DependenciesNameShortening
        title: >-
          The Query subsystem shall shorten qualified names only in the `dependencies` verb's
          Markdown output by stripping the longest leading `::`-segment prefix shared by the
          subject element and every dependency entry, while leaving JSON output fully qualified.
        justification: |
          Related dependency sets often share a long package prefix that adds no distinguishing
          value in prose output. Applying the compaction only in the one Markdown rendering path
          that needs it keeps every other verb and every JSON payload unaffected.
        tests:
          - RenderMarkdown_DependenciesVerb_NoCommonPrefix_LeavesNamesFullyQualified
          - RenderJson_DependenciesVerb_NamesRemainFullyQualified

      - id: SysML2Tools-Core-Query-QualifiedNameShortening
        title: >-
          The `QualifiedNameShortener` helper shall strip the longest leading `::`-segment
          prefix shared across a pool of qualified names, always capping the stripped length so
          every name retains at least its own leaf segment, and shall reject a `null` pool or a
          `null` pool entry.
        justification: |
          The prefix-shortening algorithm used by `DependenciesNameShortening` above has edge
          cases (single-name pools, all-identical names, differing name lengths, duplicate
          names, null inputs) that are best proven directly against the helper rather than only
          through the `dependencies` verb's Markdown rendering.
        tests:
          - QualifiedNameShortener_Shorten_OneSharedLeadingSegment_StripsThatSegment
          - QualifiedNameShortener_Shorten_NoCommonPrefix_LeavesNamesUnchanged
          - QualifiedNameShortener_Shorten_SingleNamePool_LeavesNameUnchanged
          - QualifiedNameShortener_Shorten_AllIdenticalNames_KeepsLeafSegment
          - QualifiedNameShortener_Shorten_DeeperCommonPrefix_StripsAllSharedSegments
          - QualifiedNameShortener_Shorten_ShortestNameBoundsCap_RetainsShortestNamesLeaf
          - QualifiedNameShortener_Shorten_NullPool_ThrowsArgumentNullException
          - QualifiedNameShortener_Shorten_NullEntryInPool_ThrowsArgumentNullException
          - QualifiedNameShortener_Shorten_DuplicateNamesInPool_ReturnsOneEntryPerDistinctName

      - id: SysML2Tools-Core-Query-Impact
        title: >-
          The Query subsystem shall compute the transitive closure of incoming references from a
          target element through the `impact` verb, optionally bounded by `WalkDepth` and guarded
          against cycles, excluding `Connect` and `Binding` connector edges from that reference
          closure.
        justification: |
          A single-hop reverse-reference view understates true blast radius for deeply-referenced
          elements. A bounded transitive closure gives callers a practical, terminating answer to
          "what is affected if this changes?". These reference-only semantics are the default and
          are unchanged by the opt-in connection traversal described by
          `SysML2Tools-Core-Query-ImpactConnections`; connector edges are followed only when
          `IncludeConnections` is explicitly supplied. The bound is `WalkDepth`, unlimited when
          it is not supplied, and applies uniformly to every relationship kind the walk traverses.

          Connector relationships are published into the semantic index alongside ordinary
          reference edges, so the exclusion must be explicit. Without it the default walk followed
          every connector as a plain incoming reference — directed rather than undirected, and
          attributed to the raw nested-port endpoint rather than its owning declaration. That was
          a defect: connector traversal became a direction-sensitive side effect of the reference
          walk instead of the undirected, opt-in behavior it is meant to be, and where a connector
          endpoint was a nested port the walk emitted qualified names that are not resolvable as
          query subjects. Correcting it makes the default result smaller than in releases before
          this change, and makes `IncludeConnections` a strict superset of the default.
        tests:
          - Impact_DepthOne_OnlyReachesDirectReferences
          - Impact_Unbounded_ReachesTransitiveClosure
          - Impact_IncludeConnectionsFlagAbsent_ReportsReferenceOnlyResult
          - Impact_ChainedDeclaredConnectors_FlagAbsent_ReportsNoImpactedElements
          - Impact_SourceSidePortEndpoint_FlagAbsent_ReportsNoImpactedElements

      - id: SysML2Tools-Core-Query-ImpactConnections
        title: >-
          When `IncludeConnections` is supplied, the `impact` verb shall additionally traverse
          resolved `Connect` and `Binding` connector relationships, in addition to reference
          relationships and subject to the same depth bound, without regard to their recorded
          endpoint order, so that the element at one end of a connector is reported when the
          query subject is at the other end, whichever end each occupies.
        justification: |
          A connector's two ends carry no semantic "source causes target" direction, so the
          reverse-only reference closure reports nothing for a part joined to the rest of the
          assembly only by `connect` or `bind` statements — the exact case a change-impact
          question is asked about. Answering that question correctly therefore requires the
          connector to be followed from either end, and requires the caller to opt in explicitly,
          because it broadens the answer beyond the reference-only default. The option decides
          only which relationships are part of the graph, never how far the walk goes.
        tests:
          - Impact_IncludeConnections_ReachesConnectedSiblingPart
          - Impact_IncludeConnections_QueriedFromFarEndpoint_ReachesOriginatingPart
          - Impact_IncludeConnections_BindingEdges_AreTraversedUndirected
          - Impact_IncludeConnections_ThroughPublicApi_ReturnsConnectedPartEntries

      - id: SysML2Tools-Core-Query-ImpactConnectionEndpoints
        title: >-
          The `impact` verb shall treat a connector as attached to the query subject when an
          endpoint of that connector is the subject element itself or a feature nested within it,
          and shall report the element reached at the connector's other end.
        justification: |
          Users ask about parts, but a connector's declared endpoints are normally the ports
          nested inside those parts. Unless a subject matches connectors reaching its nested
          features, asking about a part whose ports are wired to the rest of the assembly returns
          nothing, which is the opposite of the true answer.
        tests:
          - Impact_IncludeConnections_PortEndpoints_RollUpToOwningPartUsage
          - Impact_IncludeConnections_SourceSidePortEndpoint_ReportsOwnerNotRawPort
          - Impact_IncludeConnections_ReachesConnectedSiblingPart

      - id: SysML2Tools-Core-Query-ImpactConnectionRollUp
        title: >-
          The `impact` verb shall report each connector endpoint it reaches as the nearest
          declaration that owns that endpoint, and shall report the endpoint itself unchanged
          when the endpoint is already a declared element that is a valid impact subject.
        justification: |
          A nested port such as `System::hub::J1` is not itself a usable query subject, so
          reporting it raw gives the caller a name they cannot act on and cannot feed back into
          another query. Reporting the owning part instead makes every row actionable, while
          leaving an already-declared endpoint alone prevents the answer from being widened to an
          enclosing definition that the connector never named.
        tests:
          - Impact_IncludeConnections_PortEndpoints_RollUpToOwningPartUsage
          - Impact_IncludeConnections_DeclaredFarEndpoint_ReportsEndpointItself
          - Impact_IncludeConnections_DeclaredEndpointConnector_ReportsSiblingPartNotOwningDefinition
          - Impact_IncludeConnections_SourceSidePortEndpoint_ReportsOwnerNotRawPort

      - id: SysML2Tools-Core-Query-ImpactConnectionPortSkip
        title: >-
          The `impact` verb shall roll a port endpoint up to the nearest enclosing non-port
          declaration that does not contain the connector's opposite endpoint, regardless of
          whether the port endpoint is itself a declared element.
        justification: |
          A port has no behavior or state of its own to be impacted; it is the attachment point
          through which its owning part is impacted. Whether a port endpoint is itself a declared
          element is an artifact of modeling style — a port declared on a part definition and
          reached through a typed usage produces an undeclared endpoint path, while the same port
          declared inline on a part usage produces a declared one — and both styles are legal, so
          an answer that depends on which was used is not trustworthy. Reporting a port also
          truncates the analysis, because the reported element is the one the walk continues
          from, so everything wired beyond that port is silently omitted from the answer.
        tests:
          - Impact_IncludeConnections_InlineUsagePortEndpoint_ReportsOwningPartUsage
          - Impact_IncludeConnections_PortDeclarationStyles_ReportNoPortEntries
          - Impact_IncludeConnections_PortEndpoints_RollUpToOwningPartUsage

      - id: SysML2Tools-Core-Query-ImpactConnectionContainerDecline
        title: >-
          The `impact` verb shall not roll a connector endpoint up to a declaration that contains
          the connector's opposite endpoint, reporting the endpoint itself instead.
        justification: |
          A declaration that contains the opposite endpoint is a container of the element being
          expanded, not a peer of it, so reporting it would attribute containment as a
          connection. It would also curtail the analysis: because a connector is only followed
          when exactly one of its ends lies within the element being expanded, every connector
          interior to that container is discarded once the container itself is expanded, hiding
          elements genuinely wired to the subject. This arises for a proxy port declared on an
          enclosing definition, where the nearest non-port owner is the definition itself, and
          for a port declared directly in a package. Retaining the endpoint keeps the real shared
          attachment point in the answer and keeps its neighbors reachable.
        tests:
          - Impact_IncludeConnections_ProxyPortOnOwningDefinition_ReportsPortNotContainer
          - Impact_IncludeConnections_ProxyPortOnOwningDefinition_ReachesSiblingAcrossPort
          - Impact_IncludeConnections_PackageScopedPort_DoesNotTreatPackageAsConnected

      - id: SysML2Tools-Core-Query-ImpactConnectionPortContinuation
        title: >-
          The `impact` verb shall continue its traversal from the declaration a port endpoint
          rolled up to, so elements connected beyond that declaration are reached at their true
          relationship distance.
        justification: |
          Reporting and traversing must agree: if the walk advanced from the raw port instead of
          the owning part, a hub wired to several parts would yield exactly one row and every
          part on the far side of the hub would be unreachable at any depth. That is a silent
          under-report of impact, the most dangerous failure mode for a change-impact analysis,
          because the caller cannot tell an empty answer from a truncated one.
        tests:
          - Impact_IncludeConnections_InlineUsagePortEndpoint_ContinuesTraversalBeyondPortOwner
          - Impact_IncludeConnections_HubTopology_SpokeReachesOtherSpokeAtDepthTwo

      - id: SysML2Tools-Core-Query-ImpactUniformDepth
        title: >-
          The `impact` verb shall apply `WalkDepth` uniformly to every relationship it traverses,
          counting each reference relationship and each connector relationship as one unit of
          depth, and shall impose no depth bound when `WalkDepth` is not supplied.
        justification: |
          Users reason about proximity to the subject — "what is within three relationships of
          this?" — and not about per-relationship-class allowances; nobody asks for two reference
          hops and one connector hop. The class of each relationship is already reported on every
          result entry, so a caller who cares about it can group or filter on it after the fact,
          which is a presentation concern rather than a traversal budget.

          A class-specific budget also made the reported depth disagree with the true distance
          from the subject, because the two counters advanced at different rates: an element could
          be reached at a larger traversal depth but a smaller connector cost, so the depth shown
          on a row was not the number of relationships separating it from the subject. One budget
          removes that disagreement by construction.
        tests:
          - Impact_IncludeConnections_NoWalkDepth_ReachesEntireConnectorChain
          - Impact_IncludeConnections_LongChain_NoWalkDepth_ReachesFarEnd
          - Impact_IncludeConnections_WalkDepthTwo_ReachesSecondConnectionHop
          - Impact_IncludeConnections_ChainedDeclaredConnectors_WalkDepthTwo_ReachesSecondHopOnly
          - Impact_IncludeConnections_NoWalkDepth_ReachesEntireConnectorChainAtShortestDistance
          - Impact_IncludeConnections_WalkDepthThree_BoundsConnectorChainToThreeHops
          - Impact_IncludeConnections_MixedPaths_WalkDepthBoundsBothEdgeClassesIdentically
          - Impact_Summary_UnboundedWithConnections_OmitsHopClause

      - id: SysML2Tools-Core-Query-ImpactConnectionCycles
        title: >-
          The `impact` verb shall terminate and return a result for models whose connector
          relationships form cycles, including when no depth bound is supplied.
        justification: |
          Connectors are routinely declared in both textual orders between the same two parts, so
          cyclic connector topologies are ordinary models rather than corner cases. A
          change-impact query that hangs on an ordinary model is unusable, so termination is a
          user-visible obligation in its own right and not merely an implementation nicety. Since
          a depth that is not supplied is unlimited for connector relationships as well as
          reference ones,
          termination must hold with no bound in force at all and cannot rest on a bound to
          provide it.
        tests:
          - Impact_IncludeConnections_CyclicConnections_TerminatesWithoutDuplicates
          - Impact_IncludeConnections_RingTopology_NoDepthBound_Terminates
          - Impact_IncludeConnections_RingTopology_NoDepthBound_TerminatesWithShortestDistances

      - id: SysML2Tools-Core-Query-ImpactHopMinimality
        title: >-
          The `impact` verb shall report each impacted element exactly once, at its shortest
          relationship distance from the subject and the relation of the path achieving that
          distance, independently of the order in which paths are discovered.
        justification: |
          A change-impact answer that varies with traversal order cannot be relied on or
          reproduced, so the reported set — and the distance shown against each row — must be a
          function of the model and the requested bound alone. Reporting the shortest distance is
          what makes that number meaningful to a reader: "depth 2" must mean "two relationships
          away", not "two levels into whichever route the walk happened to take first".

          Reporting each element exactly once matters equally: an element reachable by several
          paths is still one element, and duplicate rows would both overstate the size of the
          blast radius and force callers to de-duplicate an answer that should already be a set.
        tests:
          - Impact_IncludeConnections_ReachedByTwoPaths_ReportsShortestDistance
          - Impact_IncludeConnections_ReachableByReferenceAndConnector_ReportedOnceAtShorterDistance
          - Impact_IncludeConnections_HubTopology_SpokeReachesOtherSpokeAtDepthTwo
          - Impact_IncludeConnections_SourceSidePortEndpoint_ProducesExactlyOneEntry

      - id: SysML2Tools-Core-Query-EntryTraversalMetadata
        title: >-
          Every traversal-produced result entry shall carry machine-readable `Depth` and
          `Relation` values, and a `ViaQualifiedName` value naming the endpoint an entry was
          attributed from whenever the reported element differs from that endpoint, alongside the
          existing human-readable `Detail` text.
        justification: |
          Non-CLI API consumers previously had to parse the free-form `Detail` string to recover a
          traversal depth, and had no way at all to tell which kind of relationship reached an
          entry — a distinction that becomes essential once reference and connector results are
          combined in one answer. Exposing the values structurally makes the API contract
          authoritative, while retaining `Detail` keeps Markdown output human-readable.
        tests:
          - Impact_ConnectionEntry_RecordsDepthRelationAndViaQualifiedName
          - Impact_ConnectionEntry_WithoutRollUp_OmitsViaQualifiedName
          - Impact_ReferenceEntry_RecordsDepthAndReferenceRelation
          - RenderMarkdown_ImpactEntry_DetailRemainsHumanReadableDepthText

      - id: SysML2Tools-Core-Query-EntryMetadataJsonOmission
        title: >-
          A result entry's `Depth`, `Relation`, and `ViaQualifiedName` values shall each be
          omitted from JSON output when that value is null.
        justification: |
          Verbs that perform no traversal populate none of these values. Emitting them as explicit
          `null`s would change the JSON payload shape of every pre-existing verb and break
          consumers that key off property presence, so a purely additive contract requires null
          values to be absent rather than present-and-null.
        tests:
          - RenderJson_EntryWithoutTraversalMetadata_OmitsDepthRelationAndVia

      - id: SysML2Tools-Core-Query-EntryRelationSerialization
        title: >-
          A result entry's `Relation` value shall be written to JSON as the name of the
          relationship kind it records.
        justification: |
          A consumer reading `"Relation": "Connect"` can distinguish "connected to" from
          "referenced by" without any lookup table, and the emitted contract stays stable if the
          underlying set of relationship kinds is ever reordered — which an ordinal encoding would
          silently break.
        tests:
          - RenderJson_EntryWithDepthAndRelation_IncludesBothFieldsAndRoundTrips

      - id: SysML2Tools-Core-Query-Describe
        title: >-
          The Query subsystem shall report a target element's own kind, qualified name, resolved
          supertypes, typing, annotations, applied metadata annotations, and a `Children` count
          through the `describe` verb, where the reported count always equals the number of
          entries actually returned (visible, named children), never the raw count of the
          element's underlying AST child nodes.
        justification: |
          A single-element fact sheet is the most common starting point for programmatic model
          exploration, consolidating information otherwise scattered across the raw source and
          semantic model. A `Children` count that disagrees with the number of rows shown (as
          happens when non-element children, such as comments/metadata/imports, or hidden
          stdlib children, are counted but not listed) misleads a reader into thinking rows are
          missing or the query is broken.
        tests:
          - Describe_ReportsKindSupertypesAnnotationsAndChildren
          - Describe_MultiLineComment_CollapsesToSingleSummaryLine
          - Describe_CommentsFixture_ReportsAnnotations
          - Describe_BareMetadataAnnotation_ReportsMetadataTypeLineOnly
          - Describe_MetadataWithScalarAttributes_ReportsOnePerAttributeLine
          - Describe_MetadataWithUnsupportedListAttribute_FallsBackToRawText
          - Describe_MultipleMetadataAnnotations_ReportsEachIndependently
          - Describe_FormatJson_IncludesMetadataInSummaryArray
          - Describe_ChildIncludesNonElementMetadataAnnotation_ChildrenCountMatchesEntryRows

      - id: SysML2Tools-Core-Query-Hierarchy
        title: >-
          The Query subsystem shall report a target element's supertype and subtype hierarchy,
          respecting the requested direction and guarding against recursive cycles.
        justification: |
          Specialization hierarchies are frequently deep and multi-branch. A dedicated traversal
          gives callers a direct answer to "how is this element related by generalization?".
        tests:
          - Hierarchy_DirectionUp_ReportsOnlySupertypes
          - Hierarchy_DirectionDown_ReportsOnlySubtypes
          - Hierarchy_DirectionBoth_ReportsSupertypesAndSubtypes
          - Hierarchy_GeneralizationExampleFixture_ReportsSupertypes

      - id: SysML2Tools-Core-Query-Requirements
        title: >-
          The Query subsystem shall report satisfy, verify, and allocate relationships where the
          target element is either the source or the target.
        justification: |
          Requirements traceability is a core systems-engineering question. The public Query API
          shall expose these resolved relationships directly from the semantic model.
        tests:
          - Requirements_ReportsSatisfyVerifyAndAllocateEdges
          - Requirements_RequirementSatisfactionFixture_ReportsAtLeastOneRelationship

      - id: SysML2Tools-Core-Query-Interface
        title: >-
          The Query subsystem shall report a target definition's ports and typed features through
          the `interface` verb.
        justification: |
          A definition's externally visible interface is what other elements connect to and type
          against. Library callers need a direct way to inspect that boundary.
        tests:
          - Interface_ReportsPortsAndTypedFeatures

      - id: SysML2Tools-Core-Query-Connections
        title: >-
          The Query subsystem shall report a target element's resolved connection endpoints,
          including dotted feature-chain resolution, and shall label each entry with the
          connector keyword that produced it.
        justification: |
          A `SysmlEdge` records only its source, target, and kind — not the connector keyword
          (`connect`/`connection`/`message`/`bind`) that produced it — so the Query subsystem
          must perform the node walk on behalf of callers that need connection topology labeled
          by the syntax that declared it. The same walk is shared with the `impact` verb's
          connector traversal so the two verbs can never disagree about connection topology.
        tests:
          - Connections_ReportsResolvedEndpointsIncludingFeatureChain
          - Connections_ConnectionsExampleFixture_ReportsAtLeastOneEndpoint

      - id: SysML2Tools-Core-Query-States
        title: >-
          The Query subsystem shall report nested states and guarded transitions reachable from a
          target element.
        justification: |
          State-machine structure is a distinct model dimension from hierarchy or containment. A
          first-class `states` verb lets callers inspect that structure without manual descendant
          traversal.
        tests:
          - States_ReportsStatesAndGuardedTransitions
          - States_StateDecompositionFixture_ReportsStatesAndTransitions

      - id: SysML2Tools-Core-Query-List
        title: >-
          The Query subsystem shall enumerate workspace declarations through the `list` verb,
          optionally filtering by kind substring and qualified-name substring, without requiring a
          target element.
        justification: |
          Workspace-wide enumeration is the discovery step that lets callers find element names
          before issuing an element-scoped query.
        tests:
          - List_NoFilters_ReturnsAllNonStdlibElements
          - List_KindFilter_OnlyMatchesGivenKind
          - List_NameFilter_OnlyMatchesGivenSubstring

      - id: SysML2Tools-Core-Query-Find
        title: >-
          The Query subsystem shall perform the same workspace-wide declaration search as `list`
          through the `find` verb, applying the supplied kind and/or name filters without
          requiring a target element.
        justification: |
          `find` is the targeted search form of workspace enumeration. Exposing it in Core keeps
          search behavior reusable for non-CLI callers.
        tests:
          - Find_WithNameFilter_Succeeds

      - id: SysML2Tools-Core-Query-StdlibFilter
        title: >-
          Every verb shall exclude stdlib-seeded elements from its results by default, and
          include them when `IncludeStdlib` is supplied on `QueryOptions`.
        justification: |
          Most model-analysis questions concern user-authored elements; hiding the large
          stdlib surface by default keeps results focused, while `IncludeStdlib` supports the
          rarer case of auditing library usage.
        tests:
          - Context_Create_QueryCommand_WithIncludeStdlibFlag_SetsIncludeStdlibTrue
          - List_IncludeStdlib_TogglesStandardLibraryVisibility

      - id: SysML2Tools-Core-Query-Exporter
        title: >-
          The Query subsystem shall provide synchronous and asynchronous file-export helpers that
          write the exact Markdown or JSON text produced by the shared renderer to a caller-
          supplied file path, without creating parent directories and without suppressing write
          failures.
        justification: |
          Library callers often want the same persisted output shape as the CLI without taking a
          dependency on CLI conventions. Keeping the file writer thin preserves a reusable Core
          API while leaving policy decisions, such as parent-directory creation and exception
          translation, to the caller.
        tests:
          - WriteMarkdown_HappyPath_MatchesRendererOutput
          - WriteMarkdown_WithDepthAndHeading_MatchesRendererOutput
          - WriteMarkdownAsync_HappyPath_MatchesRendererOutput
          - WriteJson_HappyPath_MatchesRendererOutput
          - WriteJsonAsync_HappyPath_MatchesRendererOutput
          - WriteMarkdown_MissingParentDirectory_PropagatesIoException
          - WriteJson_MissingParentDirectory_PropagatesIoException

      - id: SysML2Tools-Core-Query-EntriesLabeling
        title: >-
          For every verb except `dependencies`, Markdown rendering shall precede the entries
          table (or its "no entries" fallback) with a verb-specific bold-text label and
          fallback message naming what kind of thing the entries represent (e.g. "Children" for
          `describe`, "Uses" for `uses`), falling back to a generic "Entries"/"_No entries._"
          label for any verb without a specific mapping. The label shall always render as plain
          bold text rather than an ATX heading, so the report never branches into a Markdown
          sub-section regardless of the caller's requested heading depth.
        justification: |
          A bare, unlabeled table (or a bare "_No entries._" line) gives a reader no way to tell
          whether zero rows is an unremarkable, expected outcome for that verb or a sign the
          query is broken. Naming the entries per verb removes that ambiguity without requiring
          the reader to already know each verb's semantics by heart. Using bold text instead of
          a sub-heading keeps the whole report within a single Markdown section, which is easier
          to read and simpler to embed into other documents.
        tests:
          - RenderMarkdown_NoEntries_ReportsNoEntries
          - RenderMarkdown_EntriesPresent_IncludesVerbSpecificBoldLabel
          - RenderMarkdown_UnrecognizedVerb_FallsBackToGenericEntriesLabel
          - RenderMarkdown_ListOrFindVerb_UsesMatchingElementsLabel
          - RenderMarkdown_MaxHeadingDepth_EntriesLabelStaysBoldTextNotHeading
